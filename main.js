
n = Math.floor(Math.random() * 10 + 1);
array = Array(n).fill(0);
console.log(array);



const fgv = (arr) => {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] % 2 === 0) {
      
    }
    console.log(arr[i]);
  }

}
